# STAR-suite TODOs

This file tracks active top-level work only. Completed items and stale historical notes should stay in the implementation summaries and handoff docs, not here.

## Priority 0: Paper-Critical Validation

- Fix automatic central provenance registration for all runs (owner request, 2026-09-11; follow-up after the current process_features regression).
  - The LARRY/A375 investigation could not reliably identify the latest successful baseline because run-local records were absent from the central ledger and aggregate version labels did not match execution logs. This is exactly the failure provenance must prevent.
  - Organize records into separate `dev/` and `production/` directories under the provenance root, with a common schema and searchable index. Record the run classification explicitly; provenance is mandatory in both directories, including exploratory development runs.
  - Register production, benchmark, smoke, and diagnostic runs under stable run IDs, including failed and interrupted attempts. Record the actual binary SHA256, source revision and dirty patch, wrapper/API settings and environment overrides, input/reference/cache identities, reader/thread/output modes, completion status, phase timings, memory, validation results, and artifact locations.
  - Persist a local record before execution; automatically register start and terminal status with durable retry and an explicit registration receipt. Unregistered runs must remain visibly pending, never silently appear registered.
  - Backfill existing run records, preserve conflicting metadata through explicit corrections, and link each paper row to its exact accepted run. Keep curated public summaries distinct from the complete internal run inventory.
  - Acceptance: retrieve the latest comparable successful run and its exact executable/settings without searching scattered directories; verify registration for success, failure, interruption, and temporary service failure using tiny fixtures. Document the central location and lookup command.
  - Evidence: [LARRY/A375 investigation and provenance audit](HANDOFF_PF_SEARCH_REVERT_A375_VALIDATION_20260911.md). This entry queues the provenance fix; it does not authorize new biological benchmark runs.

- Merge the current Velocyto/BAM `packedReadInfo` lifetime fix set to `master` before the UCSF production run.
  - This is the production-critical fix for the `v0.90.1` Velocyto + BAM regression.
  - Keep the conservative lifetime guard in place for production; defer any tighter early-free optimization until it has dedicated regression coverage.
- After the UCSF production run, cut a prerelease that includes the `v0.90.1` Velocyto/BAM fix.
  - Treat the next tag as a prerelease only; do not treat it as a stable/public release yet.
  - The prerelease goal is to ensure the packaged binary path works with the production fix, not to declare final release readiness.

## Priority 1: Correctness / Parity

- Add production-scale Solo direct-bridge repeatability regression coverage.
  - The required invariant is exact raw GeneFull MEX identity for repeated
    runs with the same FASTQ input, same binary, and same production
    non-Flex inline-hash bridge flags.
  - Include a large enough A375-style perturb fixture to expose
    worker-local UMI collapse and scheduling sensitivity; tiny smoke fixtures
    are not sufficient for this bug class.
  - Compare raw and filtered MEX exactly, and record `STAR_INPUT_CHUNK_TRACE`
    or equivalent chunk ownership metadata when available so input chunking can
    be separated from downstream reduction nondeterminism.
- Create a small set of acceptable smoke-test artifacts that are safe to ship with the repo or easy to fetch from public sources.
  - Cover the main regression surfaces now exercised only with restricted host-local datasets.
  - Prefer tiny public or explicitly allowable fixtures over protected internal UCSF/Y-sensitive artifacts.
  - Include at least one fixture that can validate shared read-level lifetime dependencies such as Velocyto plus BAM CB/UB tagging.
- Do not make a stable/public release until acceptable/public regression coverage exists for the production-critical paths.
  - In particular, replace or supplement the current host-local UCSF-only Velocyto/BAM smoke with allowable/public fixtures before treating the next release as stable.
- Establish FLEX parity versus Cell Ranger 9.0 on a representative dataset.
  - This is not an immediate blocker, but it is the highest-value follow-up because other major comparisons were made against CR 9.0.
  - Include runtime comparison and inspect whether targeted acceleration in FLEX feature processing is worthwhile.
  - FLEX is currently only modestly slower than CR, so the goal is to decide whether additional complexity is justified.
- Harden mixed-chemistry (`NXT/TRU`) support now that the core behavior is implemented and E2E-validated.
  - Keep this focused on unit/smoke coverage rather than new feature design.
  - Add dedicated automated tests for:
    - mixed-chemistry merge (`NXT + TRU` feature libraries with TRU GEX)
    - 2-column mapping edge cases and chemistry precedence
    - any remaining multi-library validation gaps beyond the new filtered-cell smoke
- Tighten `packedReadInfo` lifetime after the conservative Velocyto/BAM-tagging fix.
  - Current safe behavior keeps `packedReadInfo` alive when downstream Velocyto or BAM CB/UB tagging may still consume it.
  - Follow up with a targeted post-consumer release point after `soloMain.processAndOutput()` / BAM finalization so peak RAM can drop earlier without reintroducing the regression.
- Add regression coverage for unsorted BAM CB/UB tag-injection spilling.
  - `g_unsortedTagBuffer` must always use a bounded disk-spill buffer when CB/UB tags are requested.
  - `--limitBAMsortRAM=0` must not mean "keep all unsorted tag-injection records in RAM", and unsorted-only runs must use a valid temp directory for spill files.
- Add regression coverage for the JAX/multiome production wrapper manifest loop.
  - Background remote post-MEX workers must redirect stdin from `/dev/null`.
  - SSH or child processes must not be able to consume the `while read` manifest stream and silently skip later samples.
- Implement data-driven `--clip3pPolyG auto`.

## Priority 2: Performance / Architecture

- Extend Y-removal/Y-noY emission beyond FASTQ once additional input modules
  land.
  - Current `--emitYNoYFastq yes` behavior is deliberately gated to FASTQ
    paths (`.fastq`, `.fq`, `.fastq.gz`, `.fq.gz`) while Fastx is the only
    production input module.
  - BINSEQ or other future input formats need explicit Y-removal support and
    smoke coverage before the gate is relaxed.
- Reduce the process_features decoded-record interface surface after the
  production PF CBQ input gate lands.
  - CBQ is the immediate driver, but this is a general PF input architecture
    cleanup. The shared CBQ reader keeps STAR-facing sequence packed; the PF
    adapter still materializes bounded in-memory ASCII sequence/quality lines
    because that matches the existing PF consumer path.
  - Hide newline synthesis, default-quality handling, and record-shape
    conversion behind a smaller decoded-record adapter. A narrower surface
    makes all non-FASTQ producers more robust and lets PF change its internal
    sequence representation without spreading compatibility work across every
    input module.
- Optimize JAX OCM GeneFull and Velocyto materialization using the staged plan in [docs/RUNBOOK_SCRNA_OCM_MATERIALIZATION_OPTIMIZATION_20260520.md](/mnt/pikachu/STAR-suite/docs/RUNBOOK_SCRNA_OCM_MATERIALIZATION_OPTIMIZATION_20260520.md).
  - Production should materialize `GeneFull` + Velocyto once, share OCM maps and per-sample cell calls, and skip `Gene` unless a Cell Ranger comparator run explicitly requests it.
  - Replace repeated count/write MatrixMarket scans with a streaming router that writes raw and filtered outputs from the same pass.
- Optimize the non-Flex scRNA / GEX internal gzip path using the staged harness approach in [docs/RUNBOOK_GZIP_PIPELINE_ITERATION_20260326.md](/mnt/pikachu/STAR-suite/docs/RUNBOOK_GZIP_PIPELINE_ITERATION_20260326.md).
  - Treat this as a medium-priority follow-up, not an immediate blocker: the current `zcat` workaround is acceptable for published benchmarks, but it is now clear that an optimized internal gzip path can beat `zcat` on at least one real surface (`--flexNoAlign 1` full-sample Flex).
  - Use the runbook sequence as written: Flex first, then a separate GEX harness.
  - Keep the benchmark question narrow:
    - baseline = external `zcat`
    - target = current internal gzip
    - candidate optimizations = buffered reads, delimiter scanning, and small batched handoff
  - Reuse queue telemetry / occupancy as the primary signal, and only promote a new internal path if it matches or beats the current benchmark-default `zcat` surface on the corresponding workload.

## Priority 3: Dataset / Fixture Coverage

- Benchmark PE bulk on a larger dataset with multiple samples. The current benchmark uses a single MorPHiC JAX KOLF sample (6.5M read pairs); a larger public PE dataset (50M+ pairs, multiple samples) would make the speedup more representative, provide a citable accession, and showcase batch-mode multi-sample handling.
- Fix and run the remaining smoke tests that still depend on missing fixtures for memory-leak repair validation.
- Validate `--crMinUmi` across real assay types beyond the A375 fixture:
  - lineage barcodes
  - FLEX probes
  - antibody capture

## MCP / STAR Launchpad (future)

- **User-defined STAR workflows in Launchpad** (not started): today recipes are repo-registered YAML + `config.yaml`. Parameter values can be round-tripped via client-side JSON **Load/Save parameters** only (no custom schema editor yet).
  - **Basic / high value**: templates (clone existing `star_*` schemas) and/or **paste STAR one-liner → draft YAML** + download; user commits YAML + config entry and restarts or `reload_config`. Loopback-only API acceptable.
  - **Medium**: heuristic parser for common `--flag value` pairs; document limits for multi-operand flags (`--readFilesIn`, `--outSAMtype`) and `operand_group`.
  - **Full customization**: **editor screen** to add/delete/reorder parameter rows (name, `cli_flag`, type, defaults, optional `operand_group`, `flag_order`). Validate with existing `validate_draft_workflow_schema` / `WorkflowSchema`.
  - **Persist on server** (optional, higher risk): write under a fixed repo subdir + `reload_config`, loopback-only and path-sanitized; otherwise prefer download + manual commit.

## Notes

- Flex ambiguous Hamming-1 sample-tag rescue is deferred as a low-priority follow-up (user decision, 2026-09-10). The four colorectal ambiguity sequences audited on L004 account for 19,428 CR-assigned reads and 4,379 otherwise unsupported UMIs in called cells (0.00335% of colorectal exported UMIs). All retained reads had a 26-point mismatch-quality gap (Q11 versus Q37); this is an observed minimum, not a validated acceptance threshold. The implementation appears small, but more permissive assignments and the validation time are not justified by the measured benefit now. Keep the supplied 128-entry table, fixed offset, and current rejection of ambiguous H1 matches. If revisited, evaluate confidence-based resolution while preserving tag/cell identity, validate on independent data and both CBQ/BGZF paths, and measure count/cell concordance and runtime. The other 70 ambiguous sequences and full-320K impact remain unmeasured. Evidence: [BAM/H5 audit](/mnt/pikachu/star_suite_paper/analysis/flex_tag_ambiguity_20260910/README.md); durable artifacts at `s3://star-suite-320k-benchmark-alt-171440768238-us-west-2-20260904/analysis-tools/flex_tag_ambiguity_20260910_v1/`. No implementation or benchmark is queued.
- The multisample PE bulk batch-vs-sequential equivalence work is closed for now. The current smoke/regression coverage is sufficient to catch regressions, and a broader benchmark can be revisited later if needed for formal test expansion.
- The remaining PE bulk `TranscriptVB` vs Salmon gap (`~0.06%`) is currently treated as an acceptable small policy/model difference, not an active blocker. Revisit it only if a materially larger dataset-specific gap appears or a paper/reviewer question requires exact attribution.
- The SLAM over-dispersion parameter sweep is demoted to a low-priority follow-up. The initial conservative implementation made the over-dispersion term effectively negligible; revisit it when another SLAM-seq dataset is available and there is time for a formal parameter sweep.
- Remaining CR-compatibility cleanup around ambient-window sizing and GeneFull intronic-count differences is also demoted to a low-priority reminder. Compatibility is much stronger than before; revisit only if a concrete accuracy issue or dataset-specific failure points back to these areas.
- QC graphics consolidation is deferred rather than complete. The remaining work is mainly `process_features` heatmap consolidation/optimization, and it should be revisited the next time new graphical output work is active.
- Feature-offset detection already has a preflight phase. Any deeper read-level consistency check should wait until there is a broader formal preflight framework to put it in.
- `--readFilesCommand` remains for flexibility and legacy support, but active development should assume the direct internal gzip path. If an external command path with a newer feature diverges, that is a low-priority compatibility issue rather than a current roadmap item.
- Gray-zone ambiguous-read / EM bookkeeping for EmptyDrops admission is demoted to a low-priority reminder. In principle some ambiguity signal could be recovered from BAM or a dedicated sidecar, but carrying the extra read-level state for a small number of borderline cells is not justified right now: the engineering cost is high, most of the affected barcodes are expected to be removed by downstream QC anyway, and the current UCSF evidence does not justify the complexity. Revisit only if a future dataset shows a materially larger cell-calling gain that survives downstream filtering.
- CB/UB coupling is no longer an active behavior-change task. For FLEX, treat CB/UB coupling as the intended semantics and only revisit the stats/reporting consequences if they become confusing in practice.
- Legacy FLEX `ZG/ZX` handling is demoted to dead-code cleanup rather than an active feature item. It came from the older BAM/sidecar workaround path and should only be revisited when there is time to simplify or replace it directly.
- Perturb dynamic permit scheduling is treated as complete for now. The scheduler landed, Phase 5 validation was completed, and the later PF consumer-cap removal produced the expected large throughput improvement. Keep the smoke/benchmark coverage, and only revisit if new workloads show another clear scheduling bottleneck.
- Flex already uses `libscrna`; the older migration item is complete and removed from this list.
- Linux compatibility tarballs and installer-bundle release validation are complete; release follow-up work belongs in the release/distribution docs, not here.

- A375 CbCorrector hash storage conversion is implemented and validated in the regression worktree
  (not released): khash exact/H1/ambiguity ranges plus one ordered candidate array. Full A375 fixed-four
  wall 192.10 → 145.47 s, balanced 190.60 → 146.21 s; peak RSS ~44.2 → ~39.8 GiB; exit ~28.4 → ~1 s.
  Six matrices and three guide tables match exactly in both configurations. Candidate ordering,
  FASTQ/CBQ packing and N handling pass preserved-implementation and sanitizer tests. No further
  teardown optimization is justified by the former 27–28 s gap; it is now almost entirely removed.
  Details: docs/benchmarks/A375_CB_CORRECTOR_FLAT_HASH_20260911.md.

- After committing the MSK-LARRY search corrections, implement the simple memory fixes from
  /mnt/pikachu/STAR-suite/docs/AUDIT_ALLOCATION_PATTERNS_20260911.md. Preserve the LARRY reference
  binary and count ledgers; validate each allocation change separately. Full P02 remains pending.
