#ifndef COUNTING_SINK_STRESS_H
#define COUNTING_SINK_STRESS_H

class Parameters;

/** Synthetic post-map replay into CountingSink (runMode countingSinkStress). */
void runCountingSinkStress(Parameters &P);

#endif
