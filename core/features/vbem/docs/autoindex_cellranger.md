# AutoIndex + CellRanger-Style Reference Notes

Canonical documentation lives at:

- `core/legacy/docs/autoindex_cellranger.md`

That page documents:

- CellRanger release provenance (`versionGenome` / Cell Ranger build metadata)
- Legacy vs updated filtering modes (`--cellrangerLegacyGtfFilter`)
- 47 included / 33 excluded gene-set update for `2024-A`
- Parity scope (guaranteed for formatted `genome.fa` / `genes.gtf` inputs only)
